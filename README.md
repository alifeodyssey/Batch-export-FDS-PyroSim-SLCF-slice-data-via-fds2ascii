# Batch-export-FDS-PyroSim-SLCF-slice-data-via-fds2ascii
Batch-export FDS/PyroSim SLCF slice data via fds2ascii: automates interactive prompts, runs serial time-window averaging per time point, and writes one CSV per second with variable-group support.
